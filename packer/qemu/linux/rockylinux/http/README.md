# Placeholder

Kept so packer's hardcoded `http_directory` (`packer/qemu/packer.json`,
interpolated from `distro_name`) resolves. The kickstart flow is dead:
the cloud-image builds boot via the cloud-init seed in `cloudinit/`.
